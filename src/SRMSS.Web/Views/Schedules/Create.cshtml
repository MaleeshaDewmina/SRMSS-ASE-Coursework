@model SRMSS.Web.Models.Schedule

@{
    ViewData["Title"] = "Create Schedule";
    ViewData["PageTitle"] = "Create Schedule";
    ViewData["PageSubtitle"] = "Schedule form with conflict feedback before saving.";

    var errors = ViewData.ModelState.Values
        .SelectMany(v => v.Errors)
        .Select(e => e.ErrorMessage)
        .Where(e => !string.IsNullOrWhiteSpace(e))
        .ToList();
}

<div class="schedule-create-grid">
    <div class="srmss-card">
        <h3 class="card-title">Schedule Form</h3>
        <p class="card-subtitle">Assign route, vehicle, driver and time period</p>

        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

            <div class="mb-3">
                <label asp-for="TransportRouteId" class="form-label"></label>
                <select asp-for="TransportRouteId" class="form-select" asp-items="ViewBag.TransportRouteId">
                    <option value="">-- Select Route --</option>
                </select>
                <span asp-validation-for="TransportRouteId" class="text-danger"></span>
            </div>

            <div class="mb-3">
                <label asp-for="VehicleId" class="form-label"></label>
                <select asp-for="VehicleId" class="form-select" asp-items="ViewBag.VehicleId">
                    <option value="">-- Select Vehicle --</option>
                </select>
                <span asp-validation-for="VehicleId" class="text-danger"></span>
            </div>

            <div class="mb-3">
                <label asp-for="DriverId" class="form-label"></label>
                <select asp-for="DriverId" class="form-select" asp-items="ViewBag.DriverId">
                    <option value="">-- Select Driver --</option>
                </select>
                <span asp-validation-for="DriverId" class="text-danger"></span>
            </div>

            <div class="mb-3">
                <label asp-for="ScheduleDate" class="form-label"></label>
                <input asp-for="ScheduleDate" type="date" class="form-control" />
                <span asp-validation-for="ScheduleDate" class="text-danger"></span>
            </div>

            <div class="row">
                <div class="col-md-6 mb-3">
                    <label asp-for="DepartureTime" class="form-label"></label>
                    <input asp-for="DepartureTime" type="time" class="form-control" />
                    <span asp-validation-for="DepartureTime" class="text-danger"></span>
                </div>

                <div class="col-md-6 mb-3">
                    <label asp-for="ArrivalTime" class="form-label"></label>
                    <input asp-for="ArrivalTime" type="time" class="form-control" />
                    <span asp-validation-for="ArrivalTime" class="text-danger"></span>
                </div>
            </div>

            <div class="mb-3">
                <label asp-for="Status" class="form-label"></label>
                <select asp-for="Status" class="form-select">
                    <option value="Scheduled">Scheduled</option>
                    <option value="On Time">On Time</option>
                    <option value="Delayed">Delayed</option>
                    <option value="Completed">Completed</option>
                    <option value="Cancelled">Cancelled</option>
                </select>
                <span asp-validation-for="Status" class="text-danger"></span>
            </div>

            <div class="mb-4">
                <label asp-for="Notes" class="form-label"></label>
                <textarea asp-for="Notes" class="form-control" rows="3" placeholder="Optional notes about the schedule"></textarea>
                <span asp-validation-for="Notes" class="text-danger"></span>
            </div>

            <div class="d-flex gap-3">
                <button type="submit" class="srmss-btn primary">Check and Save</button>
                <a asp-action="Index" class="srmss-btn secondary">Cancel</a>
            </div>
        </form>
    </div>

    <div class="srmss-card amber">
        <h3 class="card-title">Validation Result</h3>
        <p class="card-subtitle">Preview of smart schedule validation</p>

        @if (errors.Any())
        {
            <div class="validation-panel">
                <h3>Conflict Detected</h3>
                @foreach (var error in errors)
                {
                    <p>@error</p>
                }
            </div>

            <div class="suggestion-panel">
                <h4>Suggested actions</h4>
                <ol>
                    <li>Select another available driver</li>
                    <li>Select another available vehicle</li>
                    <li>Change departure or arrival time</li>
                </ol>
            </div>

            <a asp-action="Create" class="srmss-btn secondary w-100 mt-4">Change Time Slot</a>
        }
        else
        {
            <div class="validation-success-panel">
                <h3>Ready to Validate</h3>
                <p>
                    The system will check driver, vehicle and route conflicts before saving this schedule.
                </p>
            </div>

            <div class="suggestion-panel">
                <h4>Validation rules</h4>
                <ol>
                    <li>Arrival time must be after departure time</li>
                    <li>Driver cannot be double-booked</li>
                    <li>Vehicle cannot be double-booked</li>
                    <li>Same route cannot use the same departure time</li>
                </ol>
            </div>
        }
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}